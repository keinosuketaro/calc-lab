# calc 仕様書

文字列で書かれた数式を計算する電卓。計算の部分は標準ライブラリだけで作り（外部ライブラリは Web GUI の Flask だけ。10.2 を参照）、`eval()` / `exec()` / `ast.literal_eval()` は使わない。

## 1. 使い方

| 起動方法 | 動作 |
|---|---|
| `python -m calc` | 対話モード（REPL） |
| `python -m calc "式" ["式" ...]` | 引数を1つずつ順に計算して終了 |
| `python -m calc.web` | ブラウザで使う GUI を起動（12章） |

## 2. 式の文法

### 2.1 字句（トークン）

| 種類 | 規則 | 例 |
|---|---|---|
| 数 | `(\d+\.?\d*\|\.\d+)([eE][+-]?\d+)?` | `3` `1.5` `.5` `1.` `1e3` `2.5e-3` |
| 名前 | `[A-Za-z_][A-Za-z0-9_]*`（大文字と小文字は区別する） | `x` `rate_2` `_tmp` |
| 演算子 | `+` `-` `*` `/` `**` `(` `)` `=` | |
| 空白 | 半角スペースとタブ。トークンの間に何個あってもよく、無視する | |

- 小数点や `e` を含む数は float、それ以外の数は int とする。
- 数の直後に `_` が続く場合（`1_000`）は字句エラー「数の書き方が正しくありません」とし、数の先頭を指す。
- 上の表にない文字（`$` `%` `^` `,` 全角文字など）は字句エラー「使えない文字 'c' です」とし、その文字を指す。
- `//` は `/` が2つ並んだものとして読むので、構文エラーになる。`%` は字句エラーになる。

### 2.2 優先順位（Python と同じ）

| 強さ | 演算 | 結合 |
|---|---|---|
| 1（最強） | `( )` | |
| 2 | `**` | 右結合 |
| 3 | 単項 `-` `+` | 何個でも続けてよい（`--3` は 3） |
| 4 | `*` `/` | 左結合 |
| 5（最弱） | 二項 `+` `-` | 左結合 |

`**` の右側には単項演算子を書ける（`2 ** -1`）。したがって `-2 ** 2` は `-(2 ** 2)`、`2 ** -2 ** 2` は `2 ** (-(2 ** 2))` になる。

### 2.3 文法（EBNF）

```
line    = [ NAME "=" ] expr
expr    = term { ("+" | "-") term }
term    = unary { ("*" | "/") unary }
unary   = ("+" | "-") unary | power
power   = atom [ "**" unary ]
atom    = NUMBER | NAME | "(" expr ")"
```

- 代入は行の先頭に1回だけ書ける。`x = y = 1` や `1 + x = 2` は構文エラー。
- 掛け算の省略（`2x`、`2(3)`、`(1)(2)`）は認めない。構文エラーになる。
- 式の複雑さには上限を設ける（Python の再帰の上限で落ちないため）。超えたら構文エラー「式が複雑すぎます」。
  - **入れ子の段数は 100 まで**：`(` を開く、単項 `+` `-` を読む、`**` の右側に入る、のたびに1段深くなり、その式を読み終えると戻る。101段目に入ったトークンの位置を指す。
  - **構文木の深さは 500 まで**：数と変数名を深さ 1 とし、演算・代入のノードは「子の深さの最大 + 1」とする。`1 + 1 + … + 1` のように演算子が続く式も深くなる。深さ 501 のノードを作った演算子の位置を指す。

## 3. 計算

- 計算の結果は Python の int / float の演算結果と同じにする（`4 / 2` は `2.0`、`0.1 + 0.2` は `0.30000000000000004`）。
- 結果の表示は `str(値)` と同じ。int は桁数に上限なく表示する（`sys.set_int_max_str_digits(0)` を使う）。
- 大きな数に上限は設けない（`9 ** 9 ** 9` のような式は計算に長い時間がかかりうる）。float が表せる範囲を超えた場合は、Python が OverflowError を出すときだけ計算エラーにする。`1e400` のように Python が `inf` を返す場合は、`inf` をそのまま返す。
- ゼロ除算（`1 / 0`、`0 ** -1`）は計算エラーにする。
- 結果が複素数になる計算（`(-8) ** 0.5`）は計算エラーにする。

## 4. 変数

- `名前 = 式` で代入する。代入した値を結果として返し、表示する。
- 同じ名前に何度でも代入できる。右辺では代入前の値を使える（`x = x + 1`）。
- 未定義の変数を使うと計算エラーになる。
- 代入の右辺でエラーが起きた場合、変数は変更しない。
- 変数は、1回の起動のあいだ（REPL のセッション中、または同じコマンドの引数どうし）で共有する。
- 組み込みの定数はない。`exit` や `quit` も、行全体でなければ普通の変数名として使える。

## 5. エラー

### 5.1 種類

| 種類（表示名） | 例外クラス | 起きる場面 |
|---|---|---|
| 字句エラー | `LexError` | 使えない文字、数の書き方の誤り |
| 構文エラー | `ParseError` | 並び方の誤り、括弧の不対応、空の式 |
| 計算エラー | `EvalError` | ゼロ除算、未定義変数、複素数、オーバーフロー |

3つとも共通の基底クラス `CalcError` を継承し、次の属性を持つ。

- `message`：種類名を含まないメッセージ（下の表の文言）
- `position`：誤りの位置。入力文字列の 0 始まりのインデックス

1つの式に誤りが複数あっても、最初に見つかった1つだけを報告する。

### 5.2 メッセージと位置

| 種類 | message | position |
|---|---|---|
| 字句 | `使えない文字 'c' です` | その文字 |
| 字句 | `数の書き方が正しくありません` | 数の先頭 |
| 構文 | `式がありません` | 0 |
| 構文 | `式が途中で終わっています` | 入力の長さ（末尾の次） |
| 構文 | `')' がありません` | 入力の長さ（末尾の次） |
| 構文 | `予期しない 'tok' です` | そのトークンの先頭 |
| 構文 | `式が複雑すぎます` | 2.3 の上限を超えた位置 |
| 計算 | `ゼロで割ることはできません` | 原因の演算子（`/` または `**`） |
| 計算 | `変数 'name' は定義されていません` | 変数名の先頭 |
| 計算 | `実数の範囲で計算できません` | 原因の `**` |
| 計算 | `数が大きすぎます` | 原因の演算子 |

`'tok'` にはトークンの元の文字列が入る（例：`予期しない ')' です`）。「式が途中で終わっています」と「')' がありません」の使い分けは、入力が終わった時点で次に `)` を待っていたら後者（`(1 + 2`）、数・名前・`(` を待っていたら前者（`1 +`、`(1 +`）。

### 5.3 表示の形式

エラーは stderr に次の3行で出す。2行目は入力を半角スペース2つで字下げしたもの、3行目は `position` の位置を指す `^`。

```
計算エラー: ゼロで割ることはできません
  1 + 2 / (3 - 3)
        ^
```

`^` の前の空白の数は `2 + position` 個（入力は1行で、タブを含まないものとする）。

## 6. コマンドライン実行

- 引数を先頭から1つずつ計算し、結果を stdout に1行ずつ出す。
- エラーが起きたら、5.3 の形式で stderr に出して、そこで止める。終了コードは 1。
- すべて成功したら終了コードは 0。
- `python -m calc ""` は構文エラー「式がありません」（終了コード 1）。

## 7. 対話モード（REPL）

- stdin が端末のときは、プロンプト `> ` を出す。端末でないとき（パイプやリダイレクト）はプロンプトを出さない。
- 1行ずつ計算し、結果を stdout に、エラーを stderr（5.3 の形式）に出して続ける。
- 空行と空白だけの行は無視する。
- 行全体が `exit` または `quit`（前後の空白は無視）なら終了する。EOF（Ctrl+D）でも終了する。
- Ctrl+C は入力中の行を捨てて、次のプロンプトに戻る。
- 終了コードは、途中でエラーがあっても 0。

## 8. Python から使う API

```python
from calc import Calculator, CalcError, LexError, ParseError, EvalError

c = Calculator()
c.execute("x = 3")  # -> 3
c.execute("x * 2")  # -> 6
c.variables  # -> {"x": 3}
```

- `Calculator.execute(line: str) -> int | float`：1行を計算して結果を返す。誤りがあれば `CalcError` の子クラスを送出する。
- `Calculator.variables: dict[str, int | float]`：現在の変数。

## 9. 完成の条件

`tests/test_calc.py` に下のケースをすべてテストとして書き、次の3つを満たしたら完成とする。

1. `uv run pytest -q` がすべて成功する
2. `uv run ruff check .` で指摘がない
3. `src/calc/` に `eval` / `exec` の呼び出しがない（テストでソースを検査する）

### 9.1 計算結果（`Calculator().execute(式)` の戻り値）

型まで一致すること（`2.0` と `2` は区別する）。

| ID | 式 | 結果 |
|---|---|---|
| R-01 | `1 + 2 * 3` | `7` |
| R-02 | `(1 + 2) * 3` | `9` |
| R-03 | `10 - 4 - 3` | `3` |
| R-04 | `2 * 3 / 4` | `1.5` |
| R-05 | `7 / 2` | `3.5` |
| R-06 | `4 / 2` | `2.0` |
| R-07 | `2 ** 3 ** 2` | `512` |
| R-08 | `-2 ** 2` | `-4` |
| R-09 | `(-2) ** 2` | `4` |
| R-10 | `2 ** -1` | `0.5` |
| R-11 | `2 ** -2 ** 2` | `0.0625` |
| R-12 | `--3` | `3` |
| R-13 | `+-3` | `-3` |
| R-14 | `2 * -3` | `-6` |
| R-15 | `0.1 + 0.2` | `0.30000000000000004` |
| R-16 | `.5 + 1.` | `1.5` |
| R-17 | `1e3` | `1000.0` |
| R-18 | `2.5e-3` | `0.0025` |
| R-19 | `2 ** 100` | `1267650600228229401496703205376` |
| R-20 | `2 ** 0.5` | `1.4142135623730951` |
| R-21 | `  1\t+ 2  ` | `3` |
| R-22 | `((((1))))` | `1` |

### 9.2 変数（同じ `Calculator` で順に実行）

| ID | 実行する行（順に） | 期待 |
|---|---|---|
| V-01 | `x = 3` | 戻り値 `3`、`variables == {"x": 3}` |
| V-02 | `x = 3` → `y = x * 2 + 1` | 2行目の戻り値 `7` |
| V-03 | `x = 3` → `x = x + 1` | 2行目の戻り値 `4` |
| V-04 | `Ab = 1` → `ab = 2` → `Ab` | 3行目の戻り値 `1` |
| V-05 | `x = 3` → `x = 1 / 0` | 2行目で `EvalError`、その後も `variables["x"] == 3` |
| V-06 | `_a1 = 5` → `exit = 2` → `_a1 + exit` | 3行目の戻り値 `7` |

### 9.3 エラー（`Calculator().execute(式)` が送出する例外）

例外クラス、`message`、`position` の3つが一致すること。

| ID | 式 | 例外 | message | position |
|---|---|---|---|---|
| E-01 | `2 $ 3` | LexError | `使えない文字 '$' です` | 2 |
| E-02 | `5 % 2` | LexError | `使えない文字 '%' です` | 2 |
| E-03 | `1_000` | LexError | `数の書き方が正しくありません` | 0 |
| E-04 | `` (空) | ParseError | `式がありません` | 0 |
| E-05 | `(1 + 2` | ParseError | `')' がありません` | 6 |
| E-06 | `1 +` | ParseError | `式が途中で終わっています` | 3 |
| E-07 | `1 + 2)` | ParseError | `予期しない ')' です` | 5 |
| E-08 | `1 2` | ParseError | `予期しない '2' です` | 2 |
| E-09 | `2x` | ParseError | `予期しない 'x' です` | 1 |
| E-10 | `2(3)` | ParseError | `予期しない '(' です` | 1 |
| E-11 | `7 // 2` | ParseError | `予期しない '/' です` | 3 |
| E-12 | `x = y = 1` | ParseError | `予期しない '=' です` | 6 |
| E-13 | `1 + x = 2` | ParseError | `予期しない '=' です` | 6 |
| E-14 | `3 = 4` | ParseError | `予期しない '=' です` | 2 |
| E-15 | `x =` | ParseError | `式が途中で終わっています` | 3 |
| E-16 | `()` | ParseError | `予期しない ')' です` | 1 |
| E-17 | `1 + 2 / (3 - 3)` | EvalError | `ゼロで割ることはできません` | 6 |
| E-18 | `0 ** -1` | EvalError | `ゼロで割ることはできません` | 2 |
| E-19 | `z + 1` | EvalError | `変数 'z' は定義されていません` | 0 |
| E-20 | `(-8) ** 0.5` | EvalError | `実数の範囲で計算できません` | 5 |
| E-21 | `10.0 ** 400` | EvalError | `数が大きすぎます` | 5 |
| E-22 | `1 $ (` | LexError | `使えない文字 '$' です` | 2 |
| E-23 | `(1 +` | ParseError | `式が途中で終わっています` | 4 |

E-22 は、字句エラーが構文エラーより先に報告されることを確かめる。

### 9.4 コマンドライン（`uv run python -m calc ...` をサブプロセスで実行）

| ID | 引数 | stdout | stderr | 終了コード |
|---|---|---|---|---|
| C-01 | `"1 + 2 * 3"` | `7\n` | 空 | 0 |
| C-02 | `"x = 3" "x * 2"` | `3\n6\n` | 空 | 0 |
| C-03 | `"1 + 2 / (3 - 3)"` | 空 | `計算エラー: ゼロで割ることはできません\n  1 + 2 / (3 - 3)\n        ^\n` | 1 |
| C-04 | `"1" "2 $ 3" "4"` | `1\n` | `字句エラー: 使えない文字 '$' です\n  2 $ 3\n    ^\n` | 1 |
| C-05 | `""` | 空 | `構文エラー: 式がありません\n  \n  ^\n` | 1 |
| C-06 | `"4 / 2"` | `2.0\n` | 空 | 0 |

### 9.5 対話モード（引数なしで起動し、stdin に文字列を流し込む）

stdin は端末ではないので、プロンプトは出ない。

| ID | stdin | stdout | stderr | 終了コード |
|---|---|---|---|---|
| I-01 | `x = 3\nx * 2\n` | `3\n6\n` | 空 | 0 |
| I-02 | `1 / 0\n2 + 2\n` | `4\n` | `計算エラー: ゼロで割ることはできません\n  1 / 0\n    ^\n` | 0 |
| I-03 | `\n   \n1 + 1\n` | `2\n` | 空 | 0 |
| I-04 | `1\nexit\n2\n` | `1\n` | 空 | 0 |
| I-05 | `1\n  quit  \n2\n` | `1\n` | 空 | 0 |
| I-06 | `5`（改行なしで EOF） | `5\n` | 空 | 0 |

### 9.6 安全性

| ID | 内容 |
|---|---|
| S-01 | `src/calc/` 以下の全 `.py` を `ast` で解析し、組み込み関数 `eval` / `exec` / `compile` / `__import__` の呼び出し（`eval(...)` や `builtins.eval(...)` の形）がないこと。`re.compile(...)` など他モジュールの関数は対象外 |
| S-02 | `Calculator().execute("__import__('os')")` が `CalcError` の子クラスを送出すること（`'` は使えない文字） |

### 9.7 自動テストの対象外（手で確認する）

- 端末で起動したときにプロンプト `> ` が出ること
- Ctrl+C で入力中の行が捨てられ、REPL が続くこと
- Ctrl+D で終了すること

## 10. 設計

### 10.1 処理の流れ

```
文字列 ──tokenize──▶ list[Token] ──parse──▶ Node（構文木） ──evaluate──▶ int | float
         lexer.py                 parser.py                  evaluator.py
           │                        │                          │
           └ LexError               └ ParseError               └ EvalError
```

- 各段は前の段の出力だけを受け取る。評価器はトークンを知らず、構文解析器は文字列を知らない。
- 位置情報は Token → Node へ引き継ぐ。どの段のエラーも元の文字列の位置（`position`）を指せる。
- 3段とも状態を持たない関数にする。変数の辞書は `Calculator` が持ち、評価器には引数で渡す。

### 10.2 モジュール構成（`src/calc/`）

| モジュール | 役割 | 公開するもの | 依存先 |
|---|---|---|---|
| `errors.py` | 例外とエラー表示 | `CalcError` `LexError` `ParseError` `EvalError`、`format_error(err, source) -> str` | なし |
| `lexer.py` | 字句解析 | `TokenKind`（Enum）、`Token`、`tokenize(source) -> list[Token]` | errors |
| `nodes.py` | 構文木の型 | `Number` `Name` `UnaryOp` `BinOp` `Assign`、`dump(node) -> str` | なし |
| `parser.py` | 構文解析（再帰下降） | `parse(tokens) -> Node` | errors, lexer, nodes |
| `evaluator.py` | 評価 | `evaluate(node, variables) -> int \| float` | errors, nodes |
| `calculator.py` | 3段をつなぐ入口 | `Calculator` | lexer, parser, evaluator |
| `cli.py` | 引数実行と REPL | `main(argv, stdin, stdout, stderr) -> int` | calculator, errors |
| `__main__.py` | `python -m calc` の入口 | なし（`sys.exit(main(sys.argv[1:], ...))` を呼ぶだけ） | cli |
| `__init__.py` | 公開 API の再エクスポート | 8章の名前 | calculator, errors |
| `web.py` | Web GUI のサーバーと API（12章） | `create_app(store=None) -> Flask`、`main(argv) -> int` | calculator, errors, flask |
| `static/index.html` | Web GUI の画面（HTML・CSS・JavaScript を1ファイルに） | なし | なし（web.py が配信する） |

依存は上から下の一方向だけにし、循環させない。外部ライブラリ（Flask）に依存してよいのは `web.py` だけとし、`__init__.py` からは読み込まない（`import calc` だけでは Flask が要らないようにする）。構文木のモジュールを `ast.py` としないのは、標準ライブラリの `ast` と名前がぶつかるため。

### 10.3 データ型

```python
class TokenKind(Enum):
    NUMBER, NAME, PLUS, MINUS, STAR, SLASH, POW, LPAREN, RPAREN, ASSIGN, EOF

@dataclass(frozen=True)
class Token:
    kind: TokenKind
    text: str               # 元の文字列（エラーメッセージの 'tok' に使う）
    pos: int                # 先頭の位置（0 始まり）
    value: int | float | None = None   # NUMBER のときだけ値が入る

# 構文木（すべて frozen dataclass。pos はエラー表示に使う位置）
Number(value: int | float, pos: int)
Name(name: str, pos: int)
UnaryOp(op: str, operand: Node, pos: int)             # pos = 演算子の位置
BinOp(op: str, left: Node, right: Node, pos: int)     # pos = 演算子の位置
Assign(name: str, value: Node, pos: int)              # pos = 変数名の位置
Node = Number | Name | UnaryOp | BinOp | Assign
```

- `tokenize` は最後に必ず `EOF` トークン（`pos = len(source)`、`text = ""`）を付ける。構文解析器は「入力の終わり」を EOF の位置で報告する。
- `dump(node)` は位置を除いた S 式の文字列を返す。例：`-2 ** 2` → `(- (** 2 2))`、`x = 1 + 2` → `(= x (+ 1 2))`。テストとデバッグで構文木を読みやすく比べるために使う。

### 10.4 各段の責任

| 段 | 責任を持つこと | 持たないこと |
|---|---|---|
| lexer | 文字の分類、数の int / float の判定、`**` を1トークンにまとめる、`1_000` の検出 | 並び方の正しさ |
| parser | 優先順位と結合、代入は先頭に1回だけ、括弧の対応、余ったトークンの検出 | 値の計算、変数が定義済みかどうか |
| evaluator | 四則演算とべき乗、変数の参照と代入、Python の例外（ZeroDivisionError、OverflowError）と複素数の結果を `EvalError` に変換 | 文字列やトークンの扱い |
| calculator | 3段を順に呼ぶ、変数の辞書を持つ | 表示 |
| cli | 引数と REPL の制御、結果とエラーの表示（`format_error` を使う）、終了コード | 計算 |

- 代入が失敗したら変数を変えない（4章）ことは evaluator が守る。右辺を最後まで評価してから辞書に書き込む。
- `sys.set_int_max_str_digits(0)` は表示を担当する cli で呼ぶ。
- 評価中の `ZeroDivisionError` / `OverflowError` は、演算子ごとの `try` で捕まえて、その `BinOp.pos` を持つ `EvalError` に変換する。

## 11. テストの方針

段ごとに単体テストを置き、9章の表は受け入れテストとして端から端までを確かめる。

| テストファイル | 対象 | 入力の作り方 | 主に確かめること |
|---|---|---|---|
| `tests/test_lexer.py` | `tokenize` | 文字列 | トークンの種類・`text`・`pos`・`value`、int と float の区別、`**` と `//` の切り方、EOF の位置、字句エラー（E-01〜E-03） |
| `tests/test_parser.py` | `parse` | `tokenize` の結果 | `dump` の文字列で構文木の形（優先順位・結合・単項の連続・代入）、ノードの `pos`、構文エラー（E-04〜E-16、E-23） |
| `tests/test_evaluator.py` | `evaluate` | ノードを手で組み立てる | 演算結果と型、変数の参照・代入、代入失敗時に辞書が変わらないこと、計算エラーの `position` がノードの `pos` になること |
| `tests/test_errors.py` | `format_error` | 例外を手で作る | 3行の形式、`^` の位置（先頭・途中・末尾の次・空文字列） |
| `tests/test_calc.py` | `Calculator` | 文字列 | 9.1〜9.3、9.6 の表（受け入れテスト） |
| `tests/test_cli.py` | `main` と `python -m calc` | `io.StringIO` とサブプロセス | 9.4・9.5 の表はサブプロセスで実行。細かい分岐は `main()` に `StringIO` を渡して速く確かめる |
| `tests/test_web.py` | `calc.web` | Flask のテストクライアント | 12.3〜12.5 の API の応答、セッションの分離と上限、不正なリクエストの扱い |

- **段を切り離す**：evaluator のテストは parser を使わず、ノードを直接組み立てる。parser にバグがあっても evaluator のテストは落ちず、原因の段がすぐ分かる。parser のテストは lexer を使ってよい（lexer は先にテスト済みのため）。
- **表で書く**：同じ形のケースは `pytest.mark.parametrize` にまとめ、`id` に SPEC の ID（`R-01`、`E-17` など）を付ける。失敗したとき、SPEC のどの項目かがすぐ分かる。
- **例外の確かめ方**：`pytest.raises(クラス)` で種類を確かめ、そのあと `message` と `position` を `==` で比べる（`match=` の正規表現は使わない。`'('` などの記号がエスケープ漏れを起こしやすいため）。
- **テストを先に書く**：実装より先に、段ごとのテストを lexer → parser → evaluator → calculator → cli の順に書く。実装も同じ順に進め、各段のテストが通ってから次の段に進む。
- **サブプロセスのテストは最小限にする**：起動が遅いので、9.4・9.5 の表に限る。

## 12. Web GUI

ブラウザで使う画面。計算は 8章の `Calculator` に任せるので、結果・エラーの種類・`message`・`position` は CLI と同じになる。

### 12.1 起動

```
python -m calc.web [--host HOST] [--port PORT] [--no-browser]
```

| オプション | 既定 | 意味 |
|---|---|---|
| `--host` | `127.0.0.1` | 待ち受けるアドレス |
| `--port` | `8000` | ポート番号 |
| `--no-browser` | なし | 付けるとブラウザを開かない。付けなければ起動の約1秒後に `http://HOST:PORT/` を開く |

- Flask の開発用サーバーで動かす。手元で使う前提とし、既定では自分の端末からしか開けない。
- 3章と同じく int は桁数に上限なく返す。`create_app()` の中で `sys.set_int_max_str_digits(0)` を呼ぶ（`main()` を通さずにアプリを作った場合も同じになるように）。
- Ctrl+C で止める。

### 12.2 URL

| メソッドと URL | 動作 |
|---|---|
| `GET /` | 画面（`static/index.html`）を返す |
| `POST /api/execute` | 1行を計算する（12.3） |
| `POST /api/reset` | 変数をすべて消す（12.4） |

- 上の表にない URL は 404、URL は合っていてメソッドが違う場合は 405 を返す。

### 12.3 計算 API（`POST /api/execute`）

リクエストの本文は JSON のオブジェクト。

| キー | 型 | 必須 | 意味 |
|---|---|---|---|
| `session` | 文字列（1〜64文字） | ○ | 画面ごとの ID。変数はこの ID ごとに分けて持つ（12.5） |
| `line` | 文字列 | | 計算する1行。省略すると空文字列（構文エラー「式がありません」になる） |

式が正しくても誤りでも、ステータスは 200 で次の JSON を返す。

```json
{"ok": true, "result": "7", "variables": {"x": "3"}}
```

```json
{"ok": false, "kind": "計算エラー", "message": "ゼロで割ることはできません", "position": 6, "variables": {}}
```

| キー | 意味 |
|---|---|
| `ok` | 計算できたら `true`、`CalcError` が出たら `false` |
| `result` | 結果を `str(値)` にした文字列（3章の表示と同じ。`4 / 2` は `"2.0"`）。JavaScript の数値に直すと大きな int の桁が落ちるので、文字列で返す |
| `kind` | 5.1 の表示名（`字句エラー` `構文エラー` `計算エラー`） |
| `message`・`position` | 5.2 と同じ |
| `variables` | 計算後の変数。名前を文字コード順（大文字が先）に並べ、値は `str(値)` の文字列 |

4章と同じく、代入の右辺でエラーが起きた場合は変数を変えない。

### 12.4 リセット API（`POST /api/reset`）

- 本文は `{"session": "..."}`（12.3 と同じ条件）。
- そのセッションの変数をすべて消し、`{"ok": true, "variables": {}}` を返す。

### 12.5 セッション

- サーバーは `session` ごとに `Calculator` を1つずつ持つ。初めて見る `session` なら新しく作る。
- 持てるのは 100 セッションまで。超えたら、いちばん長く使われていないセッションを捨てる。
- セッションはサーバーのメモリにだけ置く。サーバーを止めると消える。
- 画面は、開くたびに新しい `session` を作る。したがって再読み込みすると変数は空に戻る。

### 12.6 不正なリクエスト

| 場面 | ステータス | 本文 |
|---|---|---|
| 本文が JSON のオブジェクトでない | 400 | `{"error": "リクエストは JSON のオブジェクトにしてください"}` |
| `session` がない、文字列でない、長さが 1〜64 文字でない | 400 | `{"error": "session は 1〜64 文字の文字列にしてください"}` |
| `line` が文字列でない（`/api/execute` のみ） | 400 | `{"error": "line は文字列にしてください"}` |
| 本文が 10,000 バイトを超える | 413 | Flask の既定 |

### 12.7 画面

| 部分 | 動作 |
|---|---|
| 表示部 | 直前に計算した式と、その結果を大きく出す。結果が長いときは文字を小さくする |
| 入力欄 | 式を入力する。Enter または「計算」ボタンで計算する。成功したら入力欄を空にする。空白だけのときは送らない |
| キーパッド | 数字・`.`・`e`・`x`・括弧・演算子を入力欄のカーソル位置に入れる。演算子（`+` `-` `*` `/` `**` `=`）は前後に空白を付けて入れる。`AC` で入力欄を空に、`⌫` で1文字（演算子キーで入れた「 + 」などは空白ごと）消す |
| エラー表示 | 種類とメッセージを出し、入力の `position` の文字に印を付ける（末尾の次を指すときは空白に印を付ける）。入力欄は消さず、その位置を選択する |
| 変数 | 名前と値を並べる。クリックすると名前を入力欄に入れる。「すべて消す」で 12.4 を呼ぶ |
| 履歴 | 計算した式と結果（またはエラー）を新しい順に並べる。クリックすると式を入力欄に戻す。画面の中だけに持ち、再読み込みで消える |

| キー | 動作 |
|---|---|
| Enter | 計算する（日本語入力の変換中は除く） |
| Esc | 入力欄を空にする |
| ↑ ↓ | 履歴の式を順に入力欄に呼び出す |

- ユーザーの入力とサーバーの応答は、すべて文字列として画面に入れる（HTML として解釈させない）。
- 配色とフォントは Studio K＋（https://studio-kplus.ns-laboratory.com/）にそろえる：背景 `#eee`、文字 `#432`、アクセント `#0bd`（ホバー `#0090aa`）、Montserrat と Zen Kaku Gothic Antique の細字、ピル形のボタン。フォントは Google Fonts から読み込み、読めないときは端末のゴシック体で表示する。
- 画面の幅が 820px 以下なら、電卓と「変数・履歴」を縦1列に並べる。

### 12.8 自動テストの対象外（手で確認する）

- ブラウザで画面が崩れずに表示され、キーパッド・キー操作・変数・履歴が 12.7 のとおりに動くこと
- 起動時にブラウザが開くこと
